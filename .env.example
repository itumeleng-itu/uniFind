# Copy to .env for local development. Production reads these from Secret
# Manager instead -- this file must never contain real values.

DATABASE_URL=postgres://user:password@localhost:5432/unifind
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
VERTEX_MODEL=gemini-2.5-flash
PAYSTACK_SECRET_KEY=sk_test_xxx
APP_BASE_URL=http://localhost:3000
